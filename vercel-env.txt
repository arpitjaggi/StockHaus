# Vercel Environment Variables
# Copy these into Vercel → Your Project → Settings → Environment Variables
# Replace the placeholder values with your actual values

# Backend API URL (REQUIRED)
# Get this from Railway → Your Service → Settings → Networking → Public URL
# Must include /api at the end
VITE_API_BASE_URL=https://stockhaus-production.up.railway.app/api

# Supabase Configuration (OPTIONAL - Not currently used by frontend)
# These are only needed if you want to use Supabase client-side features
# (e.g., Realtime subscriptions, direct client access)
# Currently, the frontend only uses the backend API, so these are NOT required
# Get these from Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://ybxvmphlwbdjeyndqosk.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

